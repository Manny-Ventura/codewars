let myString =
  "     what is my name zebra lixlisore nmkfld stop o32343 jkdfl w   ";

let jcase = (string) => {
  // let localIndex = 0;
  // while (localIndex < myString.length)
  // {
  // console.log(string[localIndex]);
  // localIndex++;
  // }

  let newString = myString.trim();
  console.log(newString);
};

let wiseWords = "The sunset sets at twelve o\' clock.";
ts
console.log(alphabetPosition(wiseWords));
