def first_dup(s1):
    s = s1.lower()
    values = []
    doubles = []
    for letter in s:
        if letter = 
    return answer
    if (welcome = solve):
        return counterintuitive.selectivefforts();